import React from 'react';

import {
  Button,
} from 'react-bootstrap';

function SaveButton() {
  return (
    <div>
      <Button variant="primary">
        Save Changes
      </Button>
    </div>
  );
}

export default SaveButton;
